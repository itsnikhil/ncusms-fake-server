{
  "name": "jsonserver",
  "version": "1.0.0",
  "description": "server for ncu-sms.herokuapp.com",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json": "json-server data.js -p 3500 -m authMiddleware.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0"
  }
}
